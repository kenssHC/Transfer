import { css } from "lit";

export const styles = css`
  .header {
  display: flex;
  flex-direction: column;
  gap: 8px;
 
  padding: 12px;
  }

 .subtitle {
  --text-color: var(--text-secondary);
}

`;
